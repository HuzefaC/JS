// var, let, const

var name1 = 'Zoro';

let name2 = 'Luffy'

const name = 'One Piece' // value cannot be changed and has to be initialized

console.log(name1, name2, name);

// init 

var temp;
console.log(temp);

// variable naming convention
// can include letters, numbers, $, _
// variable name cannot beign with a number